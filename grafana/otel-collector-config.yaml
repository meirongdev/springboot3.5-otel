receivers:
  otlp:
    protocols:
      http:
        endpoint: 0.0.0.0:4318

processors:
  memory_limiter:
    check_interval: 1s
    limit_mib: 256
    spike_limit_mib: 64
  batch:
    timeout: 5s
    send_batch_size: 1024
  resource/defaults:
    attributes:
      - key: service.namespace
        action: insert
        value: springboot3.5-otel
      - key: deployment.environment
        action: insert
        value: ${env:OTEL_DEPLOYMENT_ENVIRONMENT}
  attributes/sanitize:
    actions:
      # Hash user.id to preserve trace correlation ability while avoiding raw PII exposure.
      # Use hash instead of delete so correlated events can still be linked by obfuscated value.
      - key: user.id
        action: hash
      - key: user.email
        action: delete
      - key: user.phone
        action: delete

exporters:
  otlphttp/lgtm:
    endpoint: http://grafana-otel-lgtm:4318

extensions:
  health_check:
    endpoint: 0.0.0.0:13133

service:
  extensions: [health_check]
  pipelines:
    traces:
      receivers: [otlp]
      processors: [memory_limiter, resource/defaults, attributes/sanitize, batch]
      exporters: [otlphttp/lgtm]
    metrics:
      receivers: [otlp]
      processors: [memory_limiter, resource/defaults, attributes/sanitize, batch]
      exporters: [otlphttp/lgtm]
    logs:
      receivers: [otlp]
      processors: [memory_limiter, resource/defaults, attributes/sanitize, batch]
      exporters: [otlphttp/lgtm]
